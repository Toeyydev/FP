import Reservations from "@/components/Reservations";

// Operator reservation desk — inventory, direct bookings and channel cost.
export default function ReservationsPage() {
  return <Reservations />;
}
