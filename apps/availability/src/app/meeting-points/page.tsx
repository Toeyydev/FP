import MeetingPoints from "@/components/MeetingPoints";

// Operator — set meeting-point coordinates (geofencing) per tour.
export default function MeetingPointsPage() {
  return <MeetingPoints />;
}
